const buttonAnimals = document.querySelectorAll(".big-five-button");
console.log(buttonAnimals);

buttonAnimals.forEach(() => {
    button.addEventListener('click')
});


// buttonAnimals.forEach(() => {
//     btn.addEventListener('click')
// });